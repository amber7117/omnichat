version: '3.8'

services:
  backend:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    restart: always
    # Uncomment the following lines to enable live reload during development
    # volumes:
    #   - ./:/app
    #   - /app/node_modules
